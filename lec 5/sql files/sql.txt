CREATE TABLE item_details (
	item_details_id NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
	model varchar2(100),
	type_model varchar2(100),
	description varchar2(255),
	item_id NUMBER UNIQUE ,
	CONSTRAINT fk_item_id FOREIGN KEY (item_id) REFERENCES ITEM(id)
);



INSERT INTO item_details (model, type_model, description, item_id) VALUES ('2000', 'type_test', 'bla bla bla', 26);

SELECT * FROM ITEM INNER JOIN item_details  ON (ID = ITEM_ID);

SELECT * FROM ITEM i INNER JOIN item_details it ON (i.ID = it.ITEM_ID);